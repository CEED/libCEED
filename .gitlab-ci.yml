stages:
  - test

petsc:
  stage: test
  image: jedbrown/petsc:latest
  script:
    - make -j$(nproc)
    - make -k -j$(nproc) junit
  artifacts:
    paths:
      - build/*.junit
    reports:
      junit: build/*.junit
